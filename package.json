{
  "name": "taliott",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "test": "npm run test --workspace=frontend && npm run test --workspace=backend",
    "test:coverage": "npm run test:coverage --workspace=frontend && npm run test:coverage --workspace=backend",
    "test:integration": "npm run test:integration --workspace=backend",
    "test:e2e": "playwright test",
    "screenshots": "playwright test e2e/screenshots.spec.js --workers=1",
    "screenshots:readme": "node scripts/update-readme-screenshots.js",
    "setup:hooks": "git config core.hooksPath .hooks && echo 'Git hooks path set to .hooks/'"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "concurrently": "^8.2.0"
  }
}
